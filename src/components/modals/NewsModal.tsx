// import { useAppSelector } from '@/store/hook';

const NewsModal = () => {
  // const data = useAppSelector((state) => state.modals.data);

  return (
    <div className="absolute left-[50%] top-[40%] z-[9999] flex h-[30vh] w-[30vw] -translate-x-[50%] -translate-y-[50%] items-center justify-center bg-white p-4 text-black">
      news
    </div>
  );
};

export default NewsModal;
